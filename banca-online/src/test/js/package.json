{
  "name": "banca-online-client-tests",
  "version": "1.0.0",
  "description": "Client-side integration tests for banca-online",
  "scripts": {
    "test": "jest --verbose",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "node-fetch": "^2.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout": 15000,
    "verbose": true
  }
}
